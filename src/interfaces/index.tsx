export interface Link {
  item: string;
  icon: string;
  color: string;
}

export interface Profile {
  name?: string;
  id?: string;
  email: string;
  // image: string;
}

export interface userLink {
  platform: string;
  url: string;
}
